# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Android development target

For local Android builds and installs, target only the connected Pixel 10 (`arm64-v8a`). Pass `-PreactNativeArchitectures=arm64-v8a` and do not compile emulator or other device ABIs unless the user explicitly requests them.
